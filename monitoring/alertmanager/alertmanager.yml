route:
  receiver: autonomous-noc
  group_by: [alertname, service]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h
receivers:
  - name: autonomous-noc
    webhook_configs:
      - url: http://backend:8000/alerts/prometheus
        send_resolved: true

